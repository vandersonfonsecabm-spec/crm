/**
 * External provider activation is intentionally paused for the integrations
 * visibility/observability release. A future activation mission must change
 * this policy and re-run the provider safety gates before enabling writes.
 */
export const EXTERNAL_PROVIDER_ACTIVATION_ENABLED = false;
