E6A OPERATIONAL RELEASE ATTEMPT

CURRENT_STATE_AS_OF=2026-08-24 15:45 BRT
CORE_SOURCE=2b8209ef25ba28a40b756948edfcc12c07f12970
SOURCE=80a4f8d5c5067934e818f8e75f2f696d716e5ce0
DOCS=e5189ca (bundle commit; report docs 8a8c3bf)

PREFLIGHT PASS

- Railway project glistening-playfulness, production environment identified.
- API service api identified and Online at api-production-875f9.up.railway.app.
- Active API deployment is E6A merge deployment
  `1b95b3b9-f198-4a2a-91c3-998a36eb1091`, commit `80a4f8d5…`.
- PostgreSQL official target identified as Postgres-u_yI and Online.
- Vercel project crm identified; latest production deployment dpl_969... is
  READY and /health, /ready, frontend returned HTTP 200.
- AI flags are absent/default OFF in the visible API variables; no provider or
  outbound was activated. Stock/H7/H8 flags remain under the existing E5R
  production policy and were not changed by E6A.

PREVIEW BUILD

- First Vercel preview failed on TypeScript scope/nullability errors; the
  smallest UI fix was committed and pushed as `0fbd3bcb`.
- Preview deployment `dpl_7GcexRMqBfGdSWKdMBQjEKwK7eHq` is READY and has exact
  Git metadata for branch `feature/ai-commerce-e6a`.
- Latest documentation-bundle preview `dpl_85gWzvQMhk8YjrHfAJswtzRkt2sv` is
  READY at the same branch alias, with Git SHA `ae3614d118b8731dae1c6213423f1f4e25ba2f8a`.
- Browser opened the preview but authentication could not be validated on the
  preview origin. Feature-screen visual QA was not claimed.

DATABASE / MIGRATION / DEPLOY

Railway Backups for the production service display: “Upgrade to Pro to enable
Backups and PITR”. We therefore used a protected logical backup (64,627,065
bytes; SHA-256 `8fae9053…`) and restored it into isolated PostgreSQL target
`e6a_restore_20260824`. Restore/rehearsal passed; the two additive E6A
migrations were then applied to the official database and the post-migration
tenant gate passed 157/157 with zero orphaned/crossed links. API, worker and
Vercel deployed the merge SHA `80a4f8d5…` with AI OFF.

NOT CLAIMED

PRODUCTION_BACKUP=PASS_LOGICAL_PROTECTED_BACKUP
PRODUCTION_DB_PRECHECK=PASS
STOCK/AI_PROD_MIGRATION=PASS_STOCK_SCHEMA_UNCHANGED_AI_ADDITIVE
E6A_BROWSER_PROD_QA=BLOCKED_SESSION
E6A_LIVE_CANARY=BLOCKED_NO_AUTHENTICATED_CONTROLLED_TENANT
E6A_BRANCH_PUSH=PASS_REMOTE_CANDIDATE
E6A_PREVIEW_BUILD=PASS
E6A_PREVIEW_AUTHENTICATED_VISUAL_QA=BLOCKED_SESSION

SAFE RESUME

Provide an authenticated admin session for the already controlled tenant, then
run only the Mock shadow → suggestion → human approval canary and browser QA.
Do not connect a real provider, activate auto-reply, or send outbound. If no
session is available, the current production state is already the safe OFF
endpoint and no migration/deploy needs repeating.
